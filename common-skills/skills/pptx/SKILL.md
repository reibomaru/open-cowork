---
name: pptx
description: >
  PowerPoint (.pptx) スライドを生成・編集・読み取りするスキル。成果物または入力がスライドのときに使う。
  トリガー例: 「提案資料のスライドを作って」「pptx にまとめて」「このアウトラインをスライド化して」
  「既存の pptx にスライドを追加して」「デッキのテキストを差し替えて」「プレゼン資料を作って」など、
  スライド/デッキ/プレゼン/pptx/.pptx の言及や、会議・提案資料を PowerPoint 形式で求める依頼。
  near-miss（このスキルではない）:
  文章主体の文書は docx、表計算は xlsx、PDF 配布物が成果物なら pdf を使う。
---

# PowerPoint (pptx) スキル

PowerPoint プレゼンテーション (.pptx) を生成・編集する。Node.js (`pptxgenjs`) と Python (`python-pptx`) を利用できる。

## 主な用途

- 提案資料・会議資料のスライド生成 (タイトル・本文・箇条書き・表)
- 既存 pptx のテキスト差し替え・テンプレート流用
- Markdown / アウトラインからの一括スライド化

## 推奨ライブラリ

| 言語 | ライブラリ | 用途 |
|------|-----------|------|
| Node.js | `pptxgenjs` | ゼロからのスライド生成 (グローバルインストール済み) |
| Python | `python-pptx` | 既存テンプレートを開いて差分編集 |

## 出力先

生成した .pptx は `workdir/` 配下に保存する。
